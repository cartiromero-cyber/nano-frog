import "@/styles/dashboard.css";
export const metadata = { title: "Nano Frog — Reports", robots: { index: false } };
export default function L({ children }: { children: React.ReactNode }) { return <>{children}</>; }

import "@/styles/dashboard.css";
export const metadata = { title: "Nano Frog — Admin", robots: { index: false } };
export default function AdminLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
